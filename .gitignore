# IDE. This repo is public and IntelliJ IDEA's project config has no business in
# it -- it carries local paths, module layout and sometimes a run configuration
# with machine-specific values.
.idea/
*.iml

# Private working notes. Kept out of the published repo; the canonical copies
# live in the private notes repository.
.notes/
NOTES.md

# OS and editor droppings
Thumbs.db
Desktop.ini
.DS_Store
*~
*.swp
